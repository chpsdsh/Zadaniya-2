#include <stdio.h>
int main() {
    int a,b,c,d,S1,S2;
    scanf_s("%d %d %d %d",&a,&b,&c,&d);
    S1=a*b;
    S2=c*d;
    if ((a / c) >= (a / d))
        printf("Dlinnoy");
    else
        printf("Korotkoy");
    return 0;
}
